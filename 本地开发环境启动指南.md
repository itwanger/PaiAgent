# PaiAgent 本地开发环境启动指南

> 本指南适用于在本地进行 Debug 调试开发，所有服务均可设置断点调试。

---

## 📋 前置准备

### 1. 确认本地服务已安装

- [x] **MySQL 8.0+** (端口 3306, 用户: root, 密码: 123456)
- [x] **Redis** (端口 6379)
- [x] **MinIO** (端口 9000, 用户: minioadmin, 密码: minioadmin)

### 2. 确认开发工具已安装

- [x] **Python 3.11+**
- [x] **Java 21+**
- [x] **Node.js 18+**
- [x] **uv** (Python 包管理器)
- [x] **Maven 3.8+**
- [x] **PyCharm** 或 **VSCode** (Python 调试)
- [x] **IntelliJ IDEA** (Java 调试)

### 3. 安装 uv (如未安装)

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

**uv** 是一个用于管理 Python 包的工具，类似于 **pip**。安装完成后，你可以使用 **uv** 来安装和管理 Python 包。如果已经安装，可跳过。

---

## 🚀 启动步骤

### 第一步: 启动本地基础设施服务

#### 1.1 启动 MySQL

```bash
# macOS (使用 Homebrew)
brew services start mysql

# 或手动启动
mysql.server start

# 验证
mysql -h localhost -P 3306 -u root -p123456 -e "SELECT 1"
```

如果已经启动，忽略。

#### 1.2 启动 Redis

```bash
# macOS (使用 Homebrew)
brew services start redis

# 或手动启动
redis-server

# 验证
redis-cli -h localhost -p 6379 ping
# 应该返回: PONG
```

如果已经启动，忽略。

#### 1.3 启动 MinIO

```bash
# macOS (使用 Homebrew)
brew services start minio

# 或手动启动
./minio server data/

# 验证
curl http://localhost:9000/minio/health/live
# 应该返回 200 OK
```

**MinIO 初始化** (首次运行):

1. 访问 http://localhost:9000
2. 登录: minioadmin / minioadmin
3. 创建 Bucket: `console`, `workflow`, `aitools`（需要 public）

---

### 第二步: 初始化数据库

#### 2.1 初始化 MySQL 数据库

```bash
cd /Users/itwanger/Documents/GitHub/PaiAgent

# 运行初始化脚本
./scripts/init-local-mysql.sh
```

**或手动执行**:

```bash
# 创建数据库
mysql -h localhost -P 3306 -u root -p123456 -e "
CREATE DATABASE IF NOT EXISTS astron_console CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE DATABASE IF NOT EXISTS agent CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE DATABASE IF NOT EXISTS workflow CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE DATABASE IF NOT EXISTS spark-link CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
"

# 导入表结构
mysql -h localhost -P 3306 -u root -p123456 astron_console < docker/astronAgent/mysql/schema.sql
mysql -h localhost -P 3306 -u root -p123456 agent < docker/astronAgent/mysql/agent.sql
mysql -h localhost -P 3306 -u root -p123456 workflow < docker/astronAgent/mysql/workflow.sql
mysql -h localhost -P 3306 -u root -p123456 spark-link < docker/astronAgent/mysql/link.sql
```

---

### 第三步: 生成 Python 服务配置

```bash
cd /Users/itwanger/Documents/GitHub/PaiAgent

# 运行环境准备脚本
./scripts/setup-python-local-debug.sh
```

**这个脚本会自动**:
- ✅ 检查 MySQL/Redis/MinIO 连接
- ✅ 生成 3 个 Python 服务的 `config.env` 配置文件
- ✅ 配置正确的数据库密码和服务地址

**生成的配置文件位置**:
- `core/workflow/config.env`
- `core/plugin/link/config.env`
- `core/plugin/aitools/config.env`

---

### 第四步: 启动 Python 服务 (3 个服务)

你有三种方式启动 Python 服务:

#### 方式 C: 手动在多个终端启动

如果不想使用 tmux,可以在 3 个不同的终端窗口分别执行:

```bash
# 终端 1 - Link Service
cd /Users/itwanger/Documents/GitHub/PaiAgent/core/plugin/link && uv run python main.py

# 终端 2 - AITools Service  
cd /Users/itwanger/Documents/GitHub/PaiAgent/core/plugin/aitools && uv run python main.py

# 终端 3 - Workflow Service
cd /Users/itwanger/Documents/GitHub/PaiAgent/core/workflow && uv run python main.py
```

#### 方式 A: 使用 PyCharm 中调试 (推荐)

**4.1 打开 PyCharm**

```bash
# 用 PyCharm 打开项目根目录
# File -> Open -> /Users/itwanger/Documents/GitHub/PaiAgent
```

**4.2 配置 Python 解释器**

1. `File` -> `Settings` -> `Project` -> `Python Interpreter`
2. 点击 `Add Interpreter` -> `Add Local Interpreter`
3. 选择 `System Interpreter` 或使用 `uv venv` 创建虚拟环境

**4.3 创建运行配置 - Link 服务**

1. `Run` -> `Edit Configurations...`
2. 点击 `+` -> `Python`
3. 配置如下:
   - **Name**: `Link Service`
   - **Script path**: `/Users/itwanger/Documents/GitHub/PaiAgent/core/plugin/link/main.py`
   - **Working directory**: `/Users/itwanger/Documents/GitHub/PaiAgent/core/plugin/link`
   - **Environment variables**: 留空 (自动从 config.env 加载)

**4.4 创建运行配置 - AITools 服务**

1. 点击 `+` -> `Python`
2. 配置如下:
   - **Name**: `AITools Service`
   - **Script path**: `/Users/itwanger/Documents/GitHub/PaiAgent/core/plugin/aitools/main.py`
   - **Working directory**: `/Users/itwanger/Documents/GitHub/PaiAgent/core/plugin/aitools`

**4.5 创建运行配置 - Workflow 服务**

1. 点击 `+` -> `Python`
2. 配置如下:
   - **Name**: `Workflow Service`
   - **Script path**: `/Users/itwanger/Documents/GitHub/PaiAgent/core/workflow/main.py`
   - **Working directory**: `/Users/itwanger/Documents/GitHub/PaiAgent/core/workflow`

**4.6 创建运行配置 - Agent 服务**

1. 点击 `+` -> `Python`
2. 配置如下:
   - **Name**: `Agent Service`
   - **Script path**: `/Users/itwanger/Documents/GitHub/PaiAgent/core/agent/main.py`
   - **Working directory**: `/Users/itwanger/Documents/GitHub/PaiAgent/core/agent`

**4.7 按顺序启动服务** (重要!)

1. 先启动 **Link Service** (点击 Debug 按钮 🐛)
   - 等待控制台显示: `🚀 FastAPI service started successfully!`
   - 端口: 18888

2. 再启动 **AITools Service**
   - 等待启动完成
   - 端口: 18668

3. 再启动 **Workflow Service**
   - 等待启动完成
   - 端口: 7880

4. 最后启动 **Agent Service**
   - 等待启动完成
   - 端口: 17870

#### 方式 B: 使用 tmux 一键启动所有服务 (推荐)

**什么是 tmux?**

tmux 是一个终端复用器,可以在一个终端窗口中管理多个程序:
- ✅ 在一个终端同时运行 4 个 Python 服务
- ✅ 方便在不同服务间切换查看日志
- ✅ 关闭终端后服务继续运行

**4.1 安装 tmux** (如果未安装):

```bash
# macOS
brew install tmux

# 验证安装
tmux -V
```

**4.2 使用 tmux 启动所有服务**:

```bash
# 启动 tmux
tmux

# 在 tmux 中运行启动脚本
./scripts/start-all-python-services.sh
```

**预期输出**:
```
========================================
  启动所有 Python 服务
========================================

检测到 tmux 环境,将在新窗口中启动服务
✓ 所有服务已在 tmux 窗口中启动
使用 Ctrl+B, W 查看所有窗口

等待服务启动 (10秒)...

检查服务状态:
  ✓ Link 正在运行 (端口 18888)
  ✓ AITools 正在运行 (端口 18668)
  ✓ Workflow 正在运行 (端口 7880)
  ✓ Agent 正在运行 (端口 17870)

✓ 所有服务已成功启动！

服务端口:
  - Link:     http://localhost:18888
  - AITools:  http://localhost:18668
  - Workflow: http://localhost:7880
  - Agent:    http://localhost:17870

管理命令:
  - 查看服务状态: ./scripts/check-python-services.sh
  - 停止所有服务: ./scripts/stop-python-services.sh
```

**4.3 tmux 常用快捷键**:

```bash
Ctrl+B, W    # 查看所有窗口列表 (用方向键选择,Enter 进入)
Ctrl+B, N    # 切换到下一个窗口
Ctrl+B, P    # 切换到上一个窗口
Ctrl+B, 0-9  # 直接跳转到指定编号的窗口
Ctrl+B, D    # 暂时退出 tmux (服务继续运行)
```

**4.4 查看服务日志**:

```bash
# 在 tmux 中按 Ctrl+B, W
# 会显示窗口列表:
#   0: link      - Link 服务
#   1: aitools   - AITools 服务
#   2: workflow  - Workflow 服务
#   3: agent     - Agent 服务

# 使用方向键选择要查看的窗口,按 Enter 进入
# 即可看到该服务的实时日志输出
```

**4.5 恢复 tmux 会话**:

如果你之前 Ctrl+B, D 退出了 tmux:

```bash
# 恢复会话
tmux attach

# 或
tmux a
```

---

## 🔧 服务管理命令

### 检查服务状态

```bash
./scripts/check-python-services.sh
```

**输出示例**:
```
========================================
  Python 服务状态检查
========================================

✓ Link
  端口: 18888
  PID:  12345
  URL:  http://localhost:18888

✓ AITools
  端口: 18668
  PID:  12346
  URL:  http://localhost:18668

✓ Workflow
  端口: 7880
  PID:  12347
  URL:  http://localhost:7880

✓ 所有服务正在运行
```

### 停止所有服务

```bash
./scripts/stop-python-services.sh
```

**输出示例**:
```
========================================
  停止所有 Python 服务
========================================

停止 Link (PID: 12345, 端口: 18888)
✓ Link 已停止
停止 AITools (PID: 12346, 端口: 18668)
✓ AITools 已停止
停止 Workflow (PID: 12347, 端口: 7880)
✓ Workflow 已停止

✓ 已停止 3 个服务
```

### 停止单个服务

```bash
# 停止 Link 服务
kill -9 $(lsof -ti :18888)

# 停止 AITools 服务
kill -9 $(lsof -ti :18668)

# 停止 Workflow 服务
kill -9 $(lsof -ti :7880)

```

---

### 第五步: 启动 Console Hub (Java 后端)

#### 5.1 打开 IntelliJ IDEA

```bash
# 用 IDEA 打开后端项目
# File -> Open -> /Users/itwanger/Documents/GitHub/PaiAgent/console/backend
```

#### 5.2 创建 Spring Boot 运行配置

1. `Run` -> `Edit Configurations...`
2. 点击 `+` -> `Spring Boot`
3. 配置如下:
   - **Name**: `Console Hub (Local)`
   - **Main class**: `com.iflytek.astron.console.hub.HubApplication`
   - **Working directory**: `/Users/itwanger/Documents/GitHub/PaiAgent/console/backend/hub`
   - **Use classpath of module**: `hub`
   - **Active profiles**: `local`
   - **Environment variables**: 留空 (使用 application-local.yml)

#### 5.3 启动服务

1. 点击 Debug 按钮 🐛
2. 等待控制台显示: `Started HubApplication in X.XXX seconds`
3. 端口: 8080

**验证 Console Hub**:

```bash
curl http://localhost:8080/actuator/health
```

---

### 第六步: 启动前端

#### 6.1 安装依赖 (首次运行)

```bash
cd /Users/itwanger/Documents/GitHub/PaiAgent/console/frontend

# 安装依赖
npm install
```

#### 6.2 启动开发服务器

```bash
npm run dev
```

**预期输出**:

```
  VITE v5.4.0  ready in 1234 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.x.x:3000/
  ➜  press h + enter to show help
```

#### 6.3 访问应用

打开浏览器访问: **http://localhost:3000**

---

## 📊 完整服务列表

启动完成后,你应该有以下服务在运行:

| 服务 | 端口 | 访问地址 | 状态检查 |
|------|------|---------|---------|
| **基础设施** |
| MySQL | 3306 | localhost:3306 | `mysql -h localhost -P 3306 -u root -p123456 -e "SELECT 1"` |
| Redis | 6379 | localhost:6379 | `redis-cli ping` |
| MinIO | 9000 | http://localhost:9000 | `curl http://localhost:9000/minio/health/live` |
| MinIO Console | 9001 | http://localhost:9001 | 浏览器访问 |
| **Python 服务** |
| Link | 18888 | http://localhost:18888 | `curl http://localhost:18888/health` |
| AITools | 18668 | http://localhost:18668 | `curl http://localhost:18668/health` |
| Workflow | 7880 | http://localhost:7880 | `curl http://localhost:7880/health` |
| **Java 服务** |
| Console Hub | 8080 | http://localhost:8080 | `curl http://localhost:8080/actuator/health` |
| **前端** |
| Frontend | 3000 | http://localhost:3000 | 浏览器访问 |

---

## 🐛 调试技巧

### Python 服务调试

1. 在 PyCharm 中打开对应的 Python 文件
2. 在需要的地方**设置断点** (点击行号旁边)
3. 以 **Debug 模式**启动服务 (点击 🐛 按钮)
4. 触发请求,断点会自动命中
5. 可以查看变量、单步执行、评估表达式

### Console Hub 调试

1. 在 IDEA 中打开 Java 代码 (如: `WorkflowChatController.java`)
2. 在方法中**设置断点**
3. 以 **Debug 模式**启动 Console Hub
4. 通过前端或 API 工具触发请求
5. 断点命中,可以调试 Java 代码

### 前端调试

1. 打开 Chrome 浏览器
2. 按 `F12` 打开 DevTools
3. 在 `Sources` 面板中找到源代码
4. 设置断点,刷新页面即可调试
5. 支持热重载,修改代码自动刷新

---

## 🔧 常见问题

### Q1: 端口被占用怎么办?

```bash
# 查看端口占用
lsof -i :3000   # 前端
lsof -i :8080   # Console Hub
lsof -i :17870  # Agent
lsof -i :7880   # Workflow
lsof -i :18888  # Link
lsof -i :18668  # AITools

# 杀死进程
kill -9 $(lsof -ti :3000)
```

### Q2: MySQL 连接失败?

```bash
# 检查 MySQL 是否启动
mysql.server status

# 检查密码是否正确
mysql -h localhost -P 3306 -u root -p123456 -e "SELECT 1"

# 检查数据库是否存在
mysql -h localhost -P 3306 -u root -p123456 -e "SHOW DATABASES"
```

### Q3: Python 服务启动报错?

```bash
# 检查配置文件是否存在
ls -la core/agent/config.env
ls -la core/workflow/config.env
ls -la core/plugin/link/config.env
ls -la core/plugin/aitools/config.env

# 重新生成配置
./scripts/setup-python-local-debug.sh

# 检查 Python 版本
python3 --version  # 应该是 3.11+

# 检查 uv 是否安装
uv --version
```

### Q4: Console Hub 启动报错?

**检查 application-local.yml**:

```bash
cat console/backend/hub/src/main/resources/application-local.yml
```

确保包含正确的配置 (MySQL, Redis, MinIO)

**检查 Active profiles**:

在 IDEA 运行配置中,确保 `Active profiles` 设置为 `local`

### Q5: 前端无法连接后端?

**检查前端配置**:

```bash
cat console/frontend/.env.development
```

应该包含:
```
VITE_BASE_URL=http://localhost:8080
```

**检查 vite.config.js 代理配置**:

```bash
cat console/frontend/vite.config.js
```

确保 proxy 配置正确

### Q6: MinIO 无法访问?

```bash
# 检查 MinIO 是否启动
curl http://localhost:9000/minio/health/live

# 访问控制台创建 Bucket
open http://localhost:9001

# 登录: minioadmin / minioadmin
# 创建 Bucket: console, workflow, aitools
```

---

## 🎯 快速启动清单

完整的启动顺序:

```bash
# ✅ 1. 启动基础设施
brew services start mysql
brew services start redis
brew services start minio

# ✅ 2. 初始化数据库 (首次运行)
./scripts/init-local-mysql.sh

# ✅ 3. 生成 Python 配置
./scripts/setup-python-local-debug.sh

# ✅ 4. 启动 Python 服务
# 方式 A: 使用 tmux (推荐)
tmux
./scripts/start-all-python-services.sh

# 方式 B: 在 PyCharm/VSCode 中 Debug 启动

# ✅ 5. 检查服务状态
./scripts/check-python-services.sh

# ✅ 6. 在 IDEA 中启动 Console Hub
#    Active profiles: local

# ✅ 7. 启动前端
cd console/frontend && npm run dev

# ✅ 8. 访问
open http://localhost:3000
```

---

## 🎉 启动成功!

如果所有服务都正常启动,你应该能够:

1. ✅ 访问 http://localhost:3000 看到前端界面
2. ✅ 在前端创建工作流、测试 Agent
3. ✅ 在 PyCharm 中调试 Python 代码
4. ✅ 在 IDEA 中调试 Java 代码
5. ✅ 在浏览器中调试前端代码

**祝开发愉快! 🚀**

---

## 📞 获取帮助

如果遇到问题:

1. 检查所有服务是否都已启动
2. 查看控制台日志定位错误
3. 确认端口没有被占用
4. 检查配置文件是否正确
5. 重启对应的服务重试

**常用命令**:

```bash
# 查看所有 Python 进程
ps aux | grep python

# 查看所有 Java 进程  
ps aux | grep java

# 查看所有 Node 进程
ps aux | grep node

# 重启 MySQL
brew services restart mysql

# 重启 Redis
brew services restart redis

# 重启 MinIO
brew services restart minio
```
